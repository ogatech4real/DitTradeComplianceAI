# Used by some PaaS runtimes. On Render, still set this same command as
# Start Command if the dashboard shows the Django placeholder (gunicorn ...).
web: uvicorn backend.api.main:app --host 0.0.0.0 --port $PORT
